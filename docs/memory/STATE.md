# Текущее состояние

Обновлено: 2026-09-23 (итерация 011)

## Работает

**Ядро.** Единый инбокс для трёх каналов, ответы из интерфейса, таймлайн событий. Сообщение сохраняется всегда, даже при недоступном провайдере.

**Мультитенант.** Компании (`workspaces`), три роли, изоляция данных проверена на двух компаниях живым тестом. Чужие данные отдают `404`.

**Каналы VK.** Две рабочие группы подключены и слушаются. Long Poll с сохранением курсора — перезапуск сервиса сообщений не теряет. Приём и отправка используют один и тот же токен (ключ группы, если задан): так отзыв OAuth-токена в настройках сообщества не роняет приём.

**Подключение VK через OAuth.** Рядом с ручной формой — поле «Ссылка на группу ВК» и кнопка «Подключить через ВК». Приложение VK ID (его `client_id` — в `VK_APP_ID`, `backend/.env`) получает **community-токен** группы и сохраняет канал автоматически.

**Ограничение VK ID: отправка.** Токен из OAuth принимает входящие (Long Poll), но VK отклоняет его на всех `messages.*` (ошибка 1051 «метод недоступен для этого профиля»), хотя права `messages`+`manage` выданы. Поэтому к таким каналам отдельным шагом привязывается **ключ группы** — он идёт в `config.send_token` и используется для ответов. Приём при этом не страдает. Ручной флоу (ключ сразу) работает как раньше.

**Управление через UI.** Создание компании, сотрудники, подключение и удаление каналов, удаление компании, смена пароля, правка данных клиента, журнал вебхуков.

**Безопасность.** Неперебираемые URL вебхуков, журнал вебхуков за авторизацией, файлы с секретами `chmod 600`. Bootstrap-админ без явного пароля в `.env` не создаётся — нет молчаливого дефолта.

**Слой памяти и проверки.** Репозиторий git, `pre-commit` хук прогоняет четыре проверки: слой памяти, тесты валидатора, импорт бэкенда, синтаксис фронтенда. Валидатор `scripts/check_docs.py` принимает `--root` параметром. Тесты (`scripts/test_check_docs.py`) работают на копии во временной папке, боевые документы не трогают.

**Логи.** Ротация настроена, токены в логи не пишутся, ошибок нет.

## В работе

Ничего. Последняя итерация завершена, система стабильна.

## Следующее

В порядке приоритета:

1. **Сквозная проверка ответа на боевой группе.** Владелец привязывает ключ группы к OAuth-каналу и отвечает из интерфейса живому собеседнику. Механика проверена (адаптер берёт ключ, VK метод принимает), но полный путь оператора ещё не проходили.
2. **Написать в `devsupport@corp.vk.com`** про 1051 на community-токенах (текст согласован, отправляет владелец) и заодно про право `groups`. См. BACKLOG.
3. **Callback API как альтернатива Long Poll** — переключатель на канал. Нужен при росте больше 10–15 компаний: сейчас поток на каждый канал, а VK ограничивает число подключений к одной группе.
4. **Редактирование канала** — сейчас из UI только вкл/выкл, креды не поменять.
5. **Auto-reply не должен портить FRT** — сейчас автоответ бота засчитывается как первый ответ оператора. Ждём от заказчика точный список текстов автоответов.

## Передача следующему агенту

Работа шла в итерациях 009–011: подключение группы ВК через VK OAuth, переход на community-токен, отдельный ключ группы для ответов. Затронуты `backend/app/services/vk_oauth.py`, `backend/app/services/vk_connect.py`, `backend/app/routers/vk_oauth.py`, `backend/app/channels/vk.py`, `backend/app/services/pollers.py`, `frontend/app.js` и `frontend/styles.css`. Решения — ADR-021, ADR-022, ADR-023 в `DECISIONS.md`. Настройки приложения и секреты — только в `backend/.env` (не в репозитории).

Что читать первым при старте — как обычно, `AGENTS.md`, затем `PROJECT.md` и этот файл.

## Блокеры

Нет. Все внешние зависимости доступны.

## Известные ограничения

- **`MEMORY.md` в корне отсутствует в рабочем дереве** (в git-индексе есть). Удалён не в этой итерации; восстановить ли — решает владелец.
- **Пароль `probnik12345`** у тестовой компании слабый, ставился для отладки. Сменить перед реальной работой.
- **WhatsApp и Email** не проверены на живом провайдере — креденшелов не было. Логика готова, вебхуки защищены.
- **VK-токен лежит в БД открытым текстом** в `Channel.config`. Это осознанное решение ради самодостаточности канала, но при ужесточении требований надо выносить в переменные окружения.
- **Только одна рабочая компания** (`whatsapi`) с реальными клиентами. Вторая (`probnik`) — тестовая, для проверки изоляции.

## Данные

```
whatsapi   канал VK    64 диалога
probnik    канал VK     1 диалог   (тестовая)
```
